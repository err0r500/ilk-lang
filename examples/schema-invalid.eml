// THIS IS THE DOMAIN SCHEMA (INVALID)
// 1. missing timestamp generated
// 2. undeclared tag in association
// 3. query tag not associated with all events

userIdTag = Tag{userId String}

// undeclaredTag doesn't exist - should error
userRegistered = Event<userIdTag, undeclaredTag>{
    id String
    name String
    timestamp Int
}

other = Event{
    hello String
}

registerUser = Command{
    fields {
         id String
         name String
    }

    emits [userRegistered]

    // userIdTag is only on userRegistered, not other - should error
    query [
        {
            eventTypes [userRegistered, other],
            tags [userIdTag]
        }
    ]
}
